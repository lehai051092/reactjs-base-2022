import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

LoadingFeature.propTypes = {

};

function LoadingFeature(props) {
    return (
        <div>
            Loading
        </div>
    );
}

export default LoadingFeature;